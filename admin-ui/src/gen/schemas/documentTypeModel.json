{
  "type": "object",
  "properties": {
    "id": { "type": "string", "example": "dt-1" },
    "key": {
      "type": "string",
      "example": "dpa",
      "description": "URL-safe slug, immutable after creation."
    },
    "name": { "type": "string", "example": "Data Processing Agreement" },
    "external": {
      "type": "boolean",
      "example": false,
      "description": "true = externally-signed document type (SignedDocument flow, no versions/publish/gate); false = clickwrap type. Set at creation only, immutable afterwards."
    },
    "notificationTemplateId": {
      "type": "string",
      "example": "tpl-1",
      "description": "Assigned VERSION_NOTIFICATION template id."
    },
    "reminderTemplateId": {
      "type": "string",
      "example": "tpl-2",
      "description": "Assigned REMINDER template id."
    },
    "acceptanceConfirmationTemplateId": {
      "type": "string",
      "example": "tpl-3",
      "description": "Assigned ACCEPTANCE_CONFIRMATION template id."
    }
  },
  "required": ["id", "key", "name", "external"],
  "x-readme-ref-name": "DocumentTypeModel"
}
