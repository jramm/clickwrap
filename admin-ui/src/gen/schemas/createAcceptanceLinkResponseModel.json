{
  "type": "object",
  "properties": {
    "linkId": { "type": "string", "example": "al-4f6e…" },
    "url": {
      "type": "string",
      "example": "https://clickwrap.example.org/accept/3q2-…",
      "description": "Shareable URL (`${PUBLIC_BASE_URL}/accept/<token>`). The raw token appears ONLY here — the server persists just its SHA-256."
    },
    "expiresAt": { "type": "string", "format": "date-time" }
  },
  "required": ["linkId", "url", "expiresAt"],
  "x-readme-ref-name": "CreateAcceptanceLinkResponseModel"
}
