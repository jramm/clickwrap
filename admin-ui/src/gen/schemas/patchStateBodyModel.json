{
  "type": "object",
  "properties": {
    "deadlineAt": { "type": "string", "format": "date-time" },
    "suspendBlock": {
      "type": "boolean",
      "description": "true: EXPIRED_BLOCKING → NOTIFIED with a new deadlineAt (then required)."
    },
    "reopenObjection": {
      "type": "boolean",
      "description": "true: OBJECTED → NOTIFIED — reopen the objection so the customer can reconsider (evidence kept)."
    },
    "reason": { "type": "string", "example": "Customer in clarification with legal" }
  },
  "required": ["reason"],
  "x-readme-ref-name": "PatchStateBodyModel"
}
