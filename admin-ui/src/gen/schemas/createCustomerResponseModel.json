{
  "type": "object",
  "properties": {
    "id": { "type": "string", "example": "c-8a71…" },
    "externalRef": {
      "type": "string",
      "example": "crm-4711",
      "description": "Unique external reference (CRM id)."
    },
    "firstName": {
      "type": "string",
      "example": "Jane",
      "description": "Contact person's given name ('' when unknown)."
    },
    "lastName": {
      "type": "string",
      "example": "Doe",
      "description": "Contact person's family name ('' when unknown)."
    },
    "companyName": {
      "type": "string",
      "example": "Acme GmbH",
      "description": "Optional company/organisation name — preferred display label when set."
    },
    "roles": {
      "example": ["customer"],
      "description": "Audience keys.",
      "type": "array",
      "items": { "type": "string" }
    },
    "contactEmails": {
      "example": ["legal@acme.example"],
      "type": "array",
      "items": { "type": "string" }
    },
    "compliant": {
      "type": "boolean",
      "example": true,
      "description": "Compliance gate (domain semantics: false = blocked). Present on list rows only; scoped by the audience/documentType query params."
    },
    "complianceStatus": {
      "type": "string",
      "enum": ["compliant", "pending", "objected", "blocked"],
      "example": "compliant",
      "description": "Compact per-row compliance status for the list chip. Present on list rows only."
    },
    "importedAcceptances": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "versionId": { "type": "string", "example": "v-4f9f6a…" },
          "acceptanceId": { "type": "string", "example": "a-991…" }
        },
        "required": ["versionId", "acceptanceId"],
        "x-readme-ref-name": "ImportedAcceptanceModel"
      }
    }
  },
  "required": [
    "id",
    "externalRef",
    "firstName",
    "lastName",
    "roles",
    "contactEmails",
    "importedAcceptances"
  ],
  "x-readme-ref-name": "CreateCustomerResponseModel"
}
