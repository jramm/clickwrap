{
  "type": "object",
  "properties": {
    "key": {
      "type": "string",
      "example": "signed-offer",
      "description": "URL-safe slug [a-z0-9-]{2,32}."
    },
    "name": { "type": "string", "example": "Signed offer" },
    "external": {
      "type": "boolean",
      "example": false,
      "description": "true = externally-signed document type (SignedDocument flow, no versions/publish/gate). Default false (clickwrap). Immutable after creation."
    }
  },
  "required": ["key", "name"],
  "x-readme-ref-name": "CreateDocumentTypeBodyModel"
}
