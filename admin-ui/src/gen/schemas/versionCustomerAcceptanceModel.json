{
  "type": "object",
  "properties": {
    "acceptedAt": { "type": "string", "format": "date-time" },
    "method": { "type": "string", "enum": ["ACTIVE_CONSENT", "TACIT", "IMPORT"] },
    "channel": { "type": "string", "enum": ["PORTAL", "ADMIN", "SYSTEM", "LINK"] },
    "actorName": {
      "type": "string",
      "example": "Jane Doe",
      "description": "Display name of the accepting actor."
    }
  },
  "required": ["acceptedAt", "method", "channel"],
  "x-readme-ref-name": "VersionCustomerAcceptanceModel"
}
