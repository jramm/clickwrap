{
  "type": "object",
  "properties": {
    "audience": {
      "type": "string",
      "example": "customer",
      "description": "Document audience whose members would be rolled out to."
    },
    "count": {
      "type": "number",
      "example": 42,
      "description": "Number of customers that publishing this version would roll out to — customers whose roles include the document audience (the same set the publish rollout targets). Lets an admin see the impact before publishing a draft."
    }
  },
  "required": ["audience", "count"],
  "x-readme-ref-name": "AffectedCustomersModel"
}
