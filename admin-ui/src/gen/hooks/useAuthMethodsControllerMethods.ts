/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../api/kubbClient';
import type { Client, RequestConfig, ResponseErrorConfig } from '../../api/kubbClient';
import type { AuthMethodsControllerMethodsQueryResponse } from '../types/AuthMethodsControllerMethods.ts';
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query';
import { authMethodsControllerMethodsQueryResponseSchema } from '../zod/authMethodsControllerMethodsSchema.ts';
import { queryOptions, useQuery } from '@tanstack/react-query';

export const authMethodsControllerMethodsQueryKey = () => [{ url: '/admin/auth/methods' }] as const;

export type AuthMethodsControllerMethodsQueryKey = ReturnType<
  typeof authMethodsControllerMethodsQueryKey
>;

/**
 * @description Returns the login methods of the ACTIVE admin-auth strategies (env ADMIN_AUTH, ordered). Deliberately unauthenticated: the login page needs it before any credential exists.
 * @summary Login-method discovery for the admin UI (unauthenticated)
 * {@link /admin/auth/methods}
 */
export async function authMethodsControllerMethods(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    AuthMethodsControllerMethodsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: 'GET', url: `/admin/auth/methods`, ...requestConfig });
  return authMethodsControllerMethodsQueryResponseSchema.parse(res.data);
}

export function authMethodsControllerMethodsQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = authMethodsControllerMethodsQueryKey();
  return queryOptions<
    AuthMethodsControllerMethodsQueryResponse,
    ResponseErrorConfig<Error>,
    AuthMethodsControllerMethodsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return authMethodsControllerMethods({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Returns the login methods of the ACTIVE admin-auth strategies (env ADMIN_AUTH, ordered). Deliberately unauthenticated: the login page needs it before any credential exists.
 * @summary Login-method discovery for the admin UI (unauthenticated)
 * {@link /admin/auth/methods}
 */
export function useAuthMethodsControllerMethods<
  TData = AuthMethodsControllerMethodsQueryResponse,
  TQueryData = AuthMethodsControllerMethodsQueryResponse,
  TQueryKey extends QueryKey = AuthMethodsControllerMethodsQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        AuthMethodsControllerMethodsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? authMethodsControllerMethodsQueryKey();

  const query = useQuery(
    {
      ...authMethodsControllerMethodsQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
