/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../api/kubbClient';
import type { Client, RequestConfig, ResponseErrorConfig } from '../../api/kubbClient';
import type {
  AdminControllerVersionCustomersQueryResponse,
  AdminControllerVersionCustomersPathParams,
  AdminControllerVersionCustomersQueryParams,
  AdminControllerVersionCustomers401,
  AdminControllerVersionCustomers404,
} from '../types/AdminControllerVersionCustomers.ts';
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query';
import { adminControllerVersionCustomersQueryResponseSchema } from '../zod/adminControllerVersionCustomersSchema.ts';
import { queryOptions, useQuery } from '@tanstack/react-query';

export const adminControllerVersionCustomersQueryKey = (
  { id }: { id: AdminControllerVersionCustomersPathParams['id'] | undefined },
  params?: AdminControllerVersionCustomersQueryParams,
) =>
  [
    { url: '/admin/versions/:id/customers', params: { id: id } },
    ...(params ? [params] : []),
  ] as const;

export type AdminControllerVersionCustomersQueryKey = ReturnType<
  typeof adminControllerVersionCustomersQueryKey
>;

/**
 * @description Every row reports the customer state and acceptance FOR THIS version (not the currently effective one) — so drilling into an upcoming version shows who has (not) accepted THAT version. SUPERSEDED states are excluded; `acceptance` is the effective acceptance of this version only. `stats` reuses the dashboard per-version counters so the header matches the card.
 * @summary Per-version customer status list (pages of 50)
 * {@link /admin/versions/:id/customers}
 */
export async function adminControllerVersionCustomers(
  {
    id,
    params,
  }: {
    id: AdminControllerVersionCustomersPathParams['id'];
    params?: AdminControllerVersionCustomersQueryParams;
  },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    AdminControllerVersionCustomersQueryResponse,
    ResponseErrorConfig<AdminControllerVersionCustomers401 | AdminControllerVersionCustomers404>,
    unknown
  >({ method: 'GET', url: `/admin/versions/${id}/customers`, params, ...requestConfig });
  return adminControllerVersionCustomersQueryResponseSchema.parse(res.data);
}

export function adminControllerVersionCustomersQueryOptions(
  {
    id,
    params,
  }: {
    id: AdminControllerVersionCustomersPathParams['id'] | undefined;
    params?: AdminControllerVersionCustomersQueryParams;
  },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = adminControllerVersionCustomersQueryKey({ id }, params);
  return queryOptions<
    AdminControllerVersionCustomersQueryResponse,
    ResponseErrorConfig<AdminControllerVersionCustomers401 | AdminControllerVersionCustomers404>,
    AdminControllerVersionCustomersQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return adminControllerVersionCustomers(
        { id: id!, params: params },
        { ...config, signal: config.signal ?? signal },
      );
    },
  });
}

/**
 * @description Every row reports the customer state and acceptance FOR THIS version (not the currently effective one) — so drilling into an upcoming version shows who has (not) accepted THAT version. SUPERSEDED states are excluded; `acceptance` is the effective acceptance of this version only. `stats` reuses the dashboard per-version counters so the header matches the card.
 * @summary Per-version customer status list (pages of 50)
 * {@link /admin/versions/:id/customers}
 */
export function useAdminControllerVersionCustomers<
  TData = AdminControllerVersionCustomersQueryResponse,
  TQueryData = AdminControllerVersionCustomersQueryResponse,
  TQueryKey extends QueryKey = AdminControllerVersionCustomersQueryKey,
>(
  {
    id,
    params,
  }: {
    id: AdminControllerVersionCustomersPathParams['id'] | undefined;
    params?: AdminControllerVersionCustomersQueryParams;
  },
  options: {
    query?: Partial<
      QueryObserverOptions<
        AdminControllerVersionCustomersQueryResponse,
        ResponseErrorConfig<
          AdminControllerVersionCustomers401 | AdminControllerVersionCustomers404
        >,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? adminControllerVersionCustomersQueryKey({ id }, params);

  const query = useQuery(
    {
      ...adminControllerVersionCustomersQueryOptions({ id, params }, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<AdminControllerVersionCustomers401 | AdminControllerVersionCustomers404>
  > & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
