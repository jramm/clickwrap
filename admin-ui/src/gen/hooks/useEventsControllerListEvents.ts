/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../api/kubbClient';
import type { Client, RequestConfig, ResponseErrorConfig } from '../../api/kubbClient';
import type {
  EventsControllerListEventsQueryResponse,
  EventsControllerListEventsQueryParams,
  EventsControllerListEvents401,
} from '../types/EventsControllerListEvents.ts';
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query';
import { eventsControllerListEventsQueryResponseSchema } from '../zod/eventsControllerListEventsSchema.ts';
import { queryOptions, useQuery } from '@tanstack/react-query';

export const eventsControllerListEventsQueryKey = (
  params?: EventsControllerListEventsQueryParams,
) => [{ url: '/admin/events' }, ...(params ? [params] : [])] as const;

export type EventsControllerListEventsQueryKey = ReturnType<
  typeof eventsControllerListEventsQueryKey
>;

/**
 * @description A single newest-first event list for legal tracing — for the whole system or one customer — read from the append-only Event table the core writes on each successful action (dual-write alongside the unchanged evidence stores). Categories: COMMUNICATION (e-mail sent/delivered), ACCESS (hosted acceptance page opened), CONSENT (acceptances + objections), ADMINISTRATION (all admin/system actions). All filters run BEFORE pagination, so `total` is the filtered count. A date-only `to` is treated as end-of-day.
 * @summary Chronological legal event / audit log (append-only Event table, filterable, 50/page)
 * {@link /admin/events}
 */
export async function eventsControllerListEvents(
  { params }: { params?: EventsControllerListEventsQueryParams } = {},
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    EventsControllerListEventsQueryResponse,
    ResponseErrorConfig<EventsControllerListEvents401>,
    unknown
  >({ method: 'GET', url: `/admin/events`, params, ...requestConfig });
  return eventsControllerListEventsQueryResponseSchema.parse(res.data);
}

export function eventsControllerListEventsQueryOptions(
  { params }: { params?: EventsControllerListEventsQueryParams } = {},
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = eventsControllerListEventsQueryKey(params);
  return queryOptions<
    EventsControllerListEventsQueryResponse,
    ResponseErrorConfig<EventsControllerListEvents401>,
    EventsControllerListEventsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return eventsControllerListEvents(
        { params: params },
        { ...config, signal: config.signal ?? signal },
      );
    },
  });
}

/**
 * @description A single newest-first event list for legal tracing — for the whole system or one customer — read from the append-only Event table the core writes on each successful action (dual-write alongside the unchanged evidence stores). Categories: COMMUNICATION (e-mail sent/delivered), ACCESS (hosted acceptance page opened), CONSENT (acceptances + objections), ADMINISTRATION (all admin/system actions). All filters run BEFORE pagination, so `total` is the filtered count. A date-only `to` is treated as end-of-day.
 * @summary Chronological legal event / audit log (append-only Event table, filterable, 50/page)
 * {@link /admin/events}
 */
export function useEventsControllerListEvents<
  TData = EventsControllerListEventsQueryResponse,
  TQueryData = EventsControllerListEventsQueryResponse,
  TQueryKey extends QueryKey = EventsControllerListEventsQueryKey,
>(
  { params }: { params?: EventsControllerListEventsQueryParams } = {},
  options: {
    query?: Partial<
      QueryObserverOptions<
        EventsControllerListEventsQueryResponse,
        ResponseErrorConfig<EventsControllerListEvents401>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? eventsControllerListEventsQueryKey(params);

  const query = useQuery(
    {
      ...eventsControllerListEventsQueryOptions({ params }, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<EventsControllerListEvents401>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
