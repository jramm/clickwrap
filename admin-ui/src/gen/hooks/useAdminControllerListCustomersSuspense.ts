/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../api/kubbClient';
import type { Client, RequestConfig, ResponseErrorConfig } from '../../api/kubbClient';
import type {
  AdminControllerListCustomersQueryResponse,
  AdminControllerListCustomersQueryParams,
  AdminControllerListCustomers401,
} from '../types/AdminControllerListCustomers.ts';
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from '@tanstack/react-query';
import { adminControllerListCustomersQueryResponseSchema } from '../zod/adminControllerListCustomersSchema.ts';
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query';

export const adminControllerListCustomersSuspenseQueryKey = (
  params?: AdminControllerListCustomersQueryParams,
) => [{ url: '/admin/customers' }, ...(params ? [params] : [])] as const;

export type AdminControllerListCustomersSuspenseQueryKey = ReturnType<
  typeof adminControllerListCustomersSuspenseQueryKey
>;

/**
 * @description Every row carries a compliance indicator (compliant + complianceStatus) computed over the customer's states and the current published versions. `audience`/`documentType` scope that evaluation (an unknown key narrows it to nothing); `compliance` additionally filters the rows. search + compliance filters run before pagination, so `total` is the filtered count.
 * @summary List customers (paginated, 50/page, sorted by name/externalRef).
 * {@link /admin/customers}
 */
export async function adminControllerListCustomersSuspense(
  { params }: { params?: AdminControllerListCustomersQueryParams } = {},
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    AdminControllerListCustomersQueryResponse,
    ResponseErrorConfig<AdminControllerListCustomers401>,
    unknown
  >({ method: 'GET', url: `/admin/customers`, params, ...requestConfig });
  return adminControllerListCustomersQueryResponseSchema.parse(res.data);
}

export function adminControllerListCustomersSuspenseQueryOptions(
  { params }: { params?: AdminControllerListCustomersQueryParams } = {},
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = adminControllerListCustomersSuspenseQueryKey(params);
  return queryOptions<
    AdminControllerListCustomersQueryResponse,
    ResponseErrorConfig<AdminControllerListCustomers401>,
    AdminControllerListCustomersQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return adminControllerListCustomersSuspense(
        { params: params },
        { ...config, signal: config.signal ?? signal },
      );
    },
  });
}

/**
 * @description Every row carries a compliance indicator (compliant + complianceStatus) computed over the customer's states and the current published versions. `audience`/`documentType` scope that evaluation (an unknown key narrows it to nothing); `compliance` additionally filters the rows. search + compliance filters run before pagination, so `total` is the filtered count.
 * @summary List customers (paginated, 50/page, sorted by name/externalRef).
 * {@link /admin/customers}
 */
export function useAdminControllerListCustomersSuspense<
  TData = AdminControllerListCustomersQueryResponse,
  TQueryKey extends QueryKey = AdminControllerListCustomersSuspenseQueryKey,
>(
  { params }: { params?: AdminControllerListCustomersQueryParams } = {},
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        AdminControllerListCustomersQueryResponse,
        ResponseErrorConfig<AdminControllerListCustomers401>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? adminControllerListCustomersSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...adminControllerListCustomersSuspenseQueryOptions({ params }, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<AdminControllerListCustomers401>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
