/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../api/kubbClient';
import type { Client, RequestConfig, ResponseErrorConfig } from '../../api/kubbClient';
import type {
  EventsControllerListEventsQueryResponse,
  EventsControllerListEventsQueryParams,
  EventsControllerListEvents401,
} from '../types/EventsControllerListEvents.ts';
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from '@tanstack/react-query';
import { eventsControllerListEventsQueryResponseSchema } from '../zod/eventsControllerListEventsSchema.ts';
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query';

export const eventsControllerListEventsSuspenseQueryKey = (
  params?: EventsControllerListEventsQueryParams,
) => [{ url: '/admin/events' }, ...(params ? [params] : [])] as const;

export type EventsControllerListEventsSuspenseQueryKey = ReturnType<
  typeof eventsControllerListEventsSuspenseQueryKey
>;

/**
 * @description Normalizes every append-only source (admin audit log, acceptances, objections, notification events) into a single newest-first event list for legal tracing — for the whole system or one customer. Categories: COMMUNICATION (e-mail sent), ACCESS (hosted acceptance page opened), CONSENT (acceptances + objections), ADMINISTRATION (all admin/system actions). All filters run BEFORE pagination, so `total` is the filtered count. A date-only `to` is treated as end-of-day.
 * @summary Chronological legal event / audit log (aggregated, filterable, 50/page)
 * {@link /admin/events}
 */
export async function eventsControllerListEventsSuspense(
  { params }: { params?: EventsControllerListEventsQueryParams } = {},
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    EventsControllerListEventsQueryResponse,
    ResponseErrorConfig<EventsControllerListEvents401>,
    unknown
  >({ method: 'GET', url: `/admin/events`, params, ...requestConfig });
  return eventsControllerListEventsQueryResponseSchema.parse(res.data);
}

export function eventsControllerListEventsSuspenseQueryOptions(
  { params }: { params?: EventsControllerListEventsQueryParams } = {},
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = eventsControllerListEventsSuspenseQueryKey(params);
  return queryOptions<
    EventsControllerListEventsQueryResponse,
    ResponseErrorConfig<EventsControllerListEvents401>,
    EventsControllerListEventsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return eventsControllerListEventsSuspense(
        { params: params },
        { ...config, signal: config.signal ?? signal },
      );
    },
  });
}

/**
 * @description Normalizes every append-only source (admin audit log, acceptances, objections, notification events) into a single newest-first event list for legal tracing — for the whole system or one customer. Categories: COMMUNICATION (e-mail sent), ACCESS (hosted acceptance page opened), CONSENT (acceptances + objections), ADMINISTRATION (all admin/system actions). All filters run BEFORE pagination, so `total` is the filtered count. A date-only `to` is treated as end-of-day.
 * @summary Chronological legal event / audit log (aggregated, filterable, 50/page)
 * {@link /admin/events}
 */
export function useEventsControllerListEventsSuspense<
  TData = EventsControllerListEventsQueryResponse,
  TQueryKey extends QueryKey = EventsControllerListEventsSuspenseQueryKey,
>(
  { params }: { params?: EventsControllerListEventsQueryParams } = {},
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        EventsControllerListEventsQueryResponse,
        ResponseErrorConfig<EventsControllerListEvents401>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? eventsControllerListEventsSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...eventsControllerListEventsSuspenseQueryOptions({ params }, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<EventsControllerListEvents401>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
