/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../api/kubbClient';
import type { Client, RequestConfig, ResponseErrorConfig } from '../../api/kubbClient';
import type { AuthMethodsControllerMethodsQueryResponse } from '../types/AuthMethodsControllerMethods.ts';
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from '@tanstack/react-query';
import { authMethodsControllerMethodsQueryResponseSchema } from '../zod/authMethodsControllerMethodsSchema.ts';
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query';

export const authMethodsControllerMethodsSuspenseQueryKey = () =>
  [{ url: '/admin/auth/methods' }] as const;

export type AuthMethodsControllerMethodsSuspenseQueryKey = ReturnType<
  typeof authMethodsControllerMethodsSuspenseQueryKey
>;

/**
 * @description Returns the login methods of the ACTIVE admin-auth strategies (env ADMIN_AUTH, ordered). Deliberately unauthenticated: the login page needs it before any credential exists.
 * @summary Login-method discovery for the admin UI (unauthenticated)
 * {@link /admin/auth/methods}
 */
export async function authMethodsControllerMethodsSuspense(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    AuthMethodsControllerMethodsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: 'GET', url: `/admin/auth/methods`, ...requestConfig });
  return authMethodsControllerMethodsQueryResponseSchema.parse(res.data);
}

export function authMethodsControllerMethodsSuspenseQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = authMethodsControllerMethodsSuspenseQueryKey();
  return queryOptions<
    AuthMethodsControllerMethodsQueryResponse,
    ResponseErrorConfig<Error>,
    AuthMethodsControllerMethodsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return authMethodsControllerMethodsSuspense({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Returns the login methods of the ACTIVE admin-auth strategies (env ADMIN_AUTH, ordered). Deliberately unauthenticated: the login page needs it before any credential exists.
 * @summary Login-method discovery for the admin UI (unauthenticated)
 * {@link /admin/auth/methods}
 */
export function useAuthMethodsControllerMethodsSuspense<
  TData = AuthMethodsControllerMethodsQueryResponse,
  TQueryKey extends QueryKey = AuthMethodsControllerMethodsSuspenseQueryKey,
>(
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        AuthMethodsControllerMethodsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? authMethodsControllerMethodsSuspenseQueryKey();

  const query = useSuspenseQuery(
    {
      ...authMethodsControllerMethodsSuspenseQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
